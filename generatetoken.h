#pragma once
#include <iostream>

class generatetoken
{
public:
	std::string Generate0(size_t length);
	std::string Generate1(std::string::size_type length);
	std::string Generate2(std::size_t length);
};
